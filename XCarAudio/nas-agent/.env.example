API_KEY=change-me
MUSIC_DIR=/music

# Optional: enables Spotify Client Credentials fallback for public playlists.
# Works for public playlists only (private playlists still require user auth).
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
